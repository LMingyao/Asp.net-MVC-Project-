@model IEnumerable<RentalWebAppR.Models.Message>

@{
    ViewBag.Title = "Index";
}

<h2>View Users' Messages</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Message1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Employee.EmployeeUserId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tenant.TenantUserId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Message1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Employee.EmployeeUserId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tenant.TenantUserId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MessageId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MessageId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MessageId })
        </td>
    </tr>
}

</table>
